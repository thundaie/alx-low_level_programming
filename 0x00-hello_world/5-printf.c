#include <stdio.h>

/**
 * main - Entry point
 *
 * A c program that uses the printf function
 *
 * return: Always 0 (success)
 */
int main(void){
	printf ("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
