#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: printing status of n' positive, negative or zero
 *
 * Return: Always success (0)
 */

void positive_or_negative(int i)
{
	if(i == 0)
	{
		printf("%d is zero\n", i);
	}
	else if(i > 0)
	{
		printf("%d is positive\n", i);
	}
	else if
	{
		printf("%d is negative\n", i);
	}
}
