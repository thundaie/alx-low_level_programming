#include <stdio.h>

/**
 * main - entry point
 *
 * Description: Alphabet game
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
	char ch;

	for (ch = 'a'; ch <= 'z';)
	{
		putchar(ch);
		++ch;
	}
		putchar('\n');
		return (0);

}
