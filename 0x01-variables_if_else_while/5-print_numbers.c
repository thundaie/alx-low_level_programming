#include <stdio.h>

/**
 * main -Entry point
 *
 * Description: print base 10
 *
 * Return: Always 0 (success)
 */

int main(void)

{
	int num;

	for (num = 0; num < 10; ++num)
		printf("%i", num);
	printf("\n");
	return (0);
}
