#include <stdio.h>

/**
 * main- Entry point
 *
 * Description: print all except two characters
 *
 * Return: Always 0 (Success)
 */
int main(void){
	char ch;
	for(ch='a'; ch <= 'z';){
		for(ch != 'q'; && ch != 'e';){
	}
	putchar(ch);
	ch++;
}
return (0);
}

